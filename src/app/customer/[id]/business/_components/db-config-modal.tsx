import { BusinessResType } from '@/types';

export default function DbConfigModal({
  data,
  open,
  onClose
}: {
  data: BusinessResType;
  open: boolean;
  onClose: () => void;
}) {
  return <div></div>;
}
